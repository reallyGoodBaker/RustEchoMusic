[package]
name = "lyrics-plugin"
version = "0.1.0"
edition = "2021"
description = "Lyrics plugin for Rust Echo Music"

[dependencies]
plugin-sdk = { path = "../plugin-sdk" }
lofty = "0.25.1"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
