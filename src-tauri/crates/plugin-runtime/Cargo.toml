[package]
name = "plugin-runtime"
version = "0.1.0"
edition = "2021"
description = "Plugin host runtime: lifecycle state machine, DI container, contribution registry, discovery, hot-plug and failure isolation"

[dependencies]
plugin-sdk = { path = "../plugin-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
libloading = "0.9"
