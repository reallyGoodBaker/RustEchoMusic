[package]
name = "audio-gain"
version = "0.1.0"
edition = "2021"
description = "Example packaged audio plugin verifying the plugin ABI (real-time audio path)"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../../src-tauri/crates/plugin-sdk" }
serde_json = "1"

[workspace]
